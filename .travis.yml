sudo: false
language: node_js
node_js:
  - "stable"
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH=$HOME/.yarn/bin:$PATH
cache:
  yarn: true
install:
  - yarn add react
  - yarn add react-dom
  - yarn
after_success:
  - yarn semantic-release
